import { it, expect, describe } from 'vitest';

describe('group', ()=>{
  it('should', () => {
    expect(1).toBeTruthy();
  })
})

// i used "test:ui": "vitest --ui" to get a more beautiful interface for tests